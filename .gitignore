# Secrets. The VideoDB key lives here and must never be committed.
.env
app/.env

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Installed agent skills (third-party, not our source)
.agents/
skills-lock.json
